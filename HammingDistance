def HammingDistance(p, q):
    distance=0
    if len(p) != len(q):
        raise ValueError("Input strings must have the same length")
    for i in range(len(p)):
        if p[i] != q[i]:
            distance += 1
    return distance 
